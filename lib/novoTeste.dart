import 'package:flutter/material.dart';

class MyTestWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello from MyTestWidget!'),
    );
  }
}
